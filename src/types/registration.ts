export type registry = {
    name: string,
    surname: string,
}

export type privacy = {
    privacy: boolean,
    marketing: boolean,
}

export type contact = {
    email: string,
    phone: string,
}
